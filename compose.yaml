services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - MONGO_URI=${MONGO_URI}
    env_file:
      - .env
    depends_on:
      - frontend

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
