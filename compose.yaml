services:
  backend:
    build: ./backend
    ports:
      - '4000:4000'
    environment:
      - PORT=4000
      - MONGO_URI=${MONGO_URI}
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - '3000:80'  # React apps usually run on 3000
