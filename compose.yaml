services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - MONGO_URI=${MONGO_URI}
    depends_on:
      - frontend

  frontend:
    build: ./frontend
    ports:
      - "3000:80"  # host:container
